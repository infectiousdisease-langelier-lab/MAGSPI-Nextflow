# Legacy project scripts

This directory preserves the original project-specific implementation from which the generalized Nextflow workflow was refactored.

These scripts are **not executed by `main.nf`**. They are retained so that published versions of MAGSPI have a traceable development history and so users can compare the refactor against the original analysis implementation.

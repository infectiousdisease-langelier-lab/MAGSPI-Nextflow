# Software environment

`mags_pipeline.yml` is the single software environment used to build the MAGSPI container and as the optional Conda fallback.

The production workflow is intended to use Docker or Apptainer so users do not need to install the bioinformatics tools individually.
